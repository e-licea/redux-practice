const initialState = {
    movies: state.movies,
    moviesToWatch: state.moviesToWatch,
    user: state.user
}


export const moviesReducer = (state, action)=>{
    switch (action.type){
        default:
            return state;
    }
}